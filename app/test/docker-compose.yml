version: '3.8'

services:
  inels-imm-tester:
    image: inels-imm-tester
    container_name: inels-imm-tester
    networks:
      - inels-imm-network
    restart: unless-stopped

networks:
  inels-imm-network:
    external: true